start5gsde:
  description: start 5GS Digital Entity
  params:
    nrfurl:
      description: "NRF URL"
      type: string
    port:
      description: "SERVICE PORT"
      type: string
    loglevel:
      description: "LOG LEVEL"
      type: string
      default: "info"
  required:
      - nrfurl
      - port